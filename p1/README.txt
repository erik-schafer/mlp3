The code is contained in main.py

To run the code locally, at least modify line 1 of main.py to point to the "/data" folder (no trailing slash) of this subdirectory.

These python libraries are required:

sklearn
matplotlib
mpl.toolkits
numpy
pandas